# Core methylation array processing (Illumina EPIC/850K IDAT files)
# methylprep (last released 2023) needs pandas<2.0 / numpy<2.0 - it calls
# APIs (e.g. DataFrame.append) that were removed in newer releases.
# Requires Python 3.11 (see .venv setup in README) since no compatible wheel
# of these older versions exists for the newest Python.
methylprep>=1.7.1
pandas<2.0
numpy<2.0

# Data analysis
scipy>=1.10
statsmodels>=0.14

# Plotting
matplotlib>=3.7
seaborn>=0.12

# Jupyter for exploratory notebooks
jupyterlab>=4.0

# GEO metadata access
GEOparse>=2.0
